declare interface Pin {
  about: string;
  //   categories: Array<Category>;
  //   comments: Array<Comment>;
  destination: string;
  id: string;
  image: string;
  save?: {
    id;
    user: {
      id;
      name;
      image;
    };
  };
  title: string;
  user?: {
    id: string;
    name: string;
    image: string;
  };
  // userId: string;
}
